# Backend

FastAPI application for schema import, semantic matching, style extraction, and SQL export support.

Run locally:

```powershell
Copy-Item .env.example .env
python -m pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8000
```
