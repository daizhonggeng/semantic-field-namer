# Optional AI gateway settings for docker compose.
# Leave these blank if you only want local import / mapping / export flows without LLM fallback.

OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-5.4
OPENAI_TIMEOUT_SECONDS=30
OPENAI_MAX_RETRIES=2
QDRANT_API_KEY=
SENTENCE_TRANSFORMER_MODEL=BAAI/bge-small-zh-v1.5
